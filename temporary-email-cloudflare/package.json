{
  "name": "91mail-public-temporary-email",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy:first": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "deploy": "npm run deploy:first && npm run db:migrate && wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.68.0"
  }
}
